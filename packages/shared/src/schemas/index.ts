/**
 * Zod validation schemas.
 * Populated as schemas are added in subsequent specs (auth, posts, MCP tools).
 */

// Placeholder — schemas will be added in Spec 03 (auth) and Spec 07 (MCP tools)
export {};
